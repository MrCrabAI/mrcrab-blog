# Netlify serves the site at the domain root, so we do NOT set GITHUB_PAGES
# here — astro.config.mjs then builds with the root base path.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
